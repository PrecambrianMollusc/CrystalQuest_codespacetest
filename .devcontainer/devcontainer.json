{
  "name": "Python Data Viz",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
   "forwardPorts": [8888],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libosmesa6-dev && sudo apt-get update && sudo apt-get install -y libegl1-mesa-dev libgles2-mesa-dev && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python","ms-python.vscode-pylance"]
    }
  }
}

